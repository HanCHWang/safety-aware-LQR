function PrimalDual(SafeLqr,ObConsArray,epsilon)
    lambda=zeros(size(SafeLqr.h,2),SafeLqr.n);
    %global mode, for local case dual variable should be stored seperately
    %to svae memory
    
    
end